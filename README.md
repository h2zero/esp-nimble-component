# esp-nimble-component

## Using

Clone this repo into your project `components` folder using the folder name `bt`: 
```
cd project/components
git clone --recurse-submodules https://github.com/h2zero/esp-nimble-component.git bt
```
